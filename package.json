{
  "name": "mytest",
  "version": "1.0.0",
  "description": "Nuxt.js project",
  "author": "wangyangyang",
  "private": true,
  "scripts": {
    "dev": " cross-env MODE=dev nuxt",
    "online": "cross-env MODE=online nuxt build",
    "test": "cross-env MODE=dev nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate-test": "cross-env MODE=dev PORT=3030 nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "nuxt": "^2.0.0"
  },
  "config": {
    "nuxt": {
      "host": "127.0.0.1",
      "port": "3030"
    }
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "cross-env": "^5.2.0",
    "css-loader": "^2.1.1",
    "eslint": "^4.19.1",
    "eslint-friendly-formatter": "^4.0.1",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^4.0.0",
    "node-sass": "^4.11.0",
    "sass-loader": "^7.1.0"
  }
}
